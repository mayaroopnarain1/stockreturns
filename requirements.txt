# Pinned ranges — app was built & tested against streamlit 1.56, yfinance 1.3,
# pandas 2.3, altair 6, numpy 2. Upper bounds prevent silent breakage from
# major version bumps; drop the upper bound if you want bleeding-edge.
streamlit>=1.50,<2.0
yfinance>=1.2,<2.0
pandas>=2.0,<3.0
altair>=5.0,<7.0
numpy>=1.26,<3.0
